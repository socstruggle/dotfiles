# Ubuntu 20.04.6 first install
sudo apt update && sudo apt upgrade
sudo dpkg --add-architecture i386 && sudo apt update && sudo apt upgrade 
sudo apt install iproute2 gawk python3 python git vim tmux build-essential \
gcc make net-tools libncurses5-dev atftpd zlib1g-dev libssl-dev \
flex bison libselinux1 gnupg wget git-core diffstat chrpath \
socat xterm autoconf libtool tar unzip texinfo gcc-multilib \
automake zlib1g:i386 screen pax gzip cpio python3-pip \
python3-pexpect xz-utils debianutils iputils-ping \
python3-git python3-jinja2 libegl1-mesa libsdl1.2-dev pylint3 \
nfs-kernel-server nfs-common libstdc++6:i386 libgtk2.0-0:i386 \
dpkg-dev:i386 libtinfo5 libncurses5 libfontconfig1:i386 \
libx11-6:i386 libxext6:i386 libxrender1:i386 libsm6:i386 \
weechat x2goclient keepassxc slim suckless-tools tclsh \
tasksel graphviz xvfb libncursesw5 lsb xfce4 xfce4-terminal xfce4-goodies \
firefox gstreamer1.0-alsa gstreamer1.0-gl gstreamer1.0-gtk3 gstreamer1.0-plugins-base \
gstreamer1.0-plugins-base-apps gstreamer1.0-plugins-good gstreamer1.0-pulseaudio \
gstreamer1.0-tools gstreamer1.0-x libgstreamer1.0-0 libgstreamer1.0-dev gstreamer1.0-libav \
gstreamer1.0-nice gstreamer1.0-opencv gstreamer1.0-pipewire gstreamer1.0-plugins-bad \
gstreamer1.0-plugins-rtp gstreamer1.0-plugins-ugly gstreamer1.0-qt5 \
gstreamer1.0-rtsp gstreamer1.0-vaapi brightnessctl brightness-udev \
gstreamer1.0-omx-generic gstreamer1.0-omx-generic-config \
bmon wireshark tcpdump tshark atop htop slurm tcptrack iftop nethogs speedometer \
python3-numpy python3-all python3-cairo gstreamer1.0-python3-plugin-loader \
python3-dbus python3 iptraf ifstat sysstat glances vnstat nload wavemon conky-all
sudo dpkg-reconfigure dash
git config --global user.name "Cem Kayhan"
git config --global user.email cemkayhan42@gmail.com

# Create users & groups
sudo groupadd xilinx
sudo adduser aalniak
sudo adduser aberke
sudo adduser hguler
sudo adduser boral
sudo usermod -aG xilinx ckayhan
sudo usermod -aG xilinx aalniak
sudo usermod -aG xilinx aberke
sudo usermod -aG xilinx hguler
sudo usermod -aG xilinx boral

# Vitis 2022.2 installation
sudo mkdir -p /opt/Vitis
sudo chown -R ckayhan:ckayhan /opt/Vitis
./xsetup -b ConfigGen
./xsetup --agree XilinxEULA,3rdPartyEULA --batch Install --config ~/.Xilinx/install_config.txt
sudo chown -R root:xilinx /opt/Vitis

# Petalinux 2022.2 installation
sudo mkdir -p /opt/Petalinux/2022.2
sudo chown -R ckayhan:ckayhan /opt/Petalinux
~/petalinux*.run -d /opt/Petalinux/2022.2
sudo chown -R root:xilinx /opt/Petalinux

##sudo apt install -y wireshark python3-pip git vim tmux lib32stdc++6 libgtk2.0-0:i386 libfontconfig1:i386 libx11-6:i386 libxext6:i386 \
##libxrender1:i386 libsm6:i386 libqtgui4:i386 libstdc++6:i386 libgtk2.0-0:i386 dpkg-dev:i386 libtinfo5 libncurses5 tcl \
##autoconf make gcc nmap build-essential automake minicom gcc git make net-tools libncurses5-dev atftpd zlib1g-dev libssl-dev flex \
##bison libselinux1 gnupg nfs-kernel-server file-roller wget diffstat chrpath socat xterm autoconf libtool tar unzip texinfo rlwrap httrack aria2 faketime \
##zlib1g-dev gcc-multilib nfs-common portmap gnome-system-monitor build-essential libsdl1.2-dev libglib2.0-dev build-essential zlib1g:i386 screen pax gzip vim-gtk3
##sudo dpkg-reconfigure dash
##sudo ln -s /usr/bin/make /usr/bin/gmake
##sudo sed -e 's/^USE_INETD=true/USE_INETD=false/g' -i /etc/default/atftpd
##sudo sed -e 's/\/srv\/tftp/\/var\/lib\/tftpboot/g' -i /etc/default/atftpd
##sudo mkdir -p /var/lib/tftpboot
##sudo chown -R nobody:nogroup /var/lib/tftpboot
##sudo chmod -R 777 /var/lib/tftpboot
##sudo systemctl enable atftpd # remote any lines starting with "tftp" from /etc/inetd.conf
###sudo systemctl restart atftpd
##sudo reboot
##sudo add-apt-repository ppa:jonathonf/vim && sudo apt update && sudo apt upgrade
##sudo add-apt-repository ppa:x2go/stable && sudo apt update && sudo apt install x2goserver x2goserver-xsession
##sudo cp -f /lib/terminfo/x/xterm /usr/share/terminfo/x
##sudo mkdir -p /opt/Xilinx/Petalinux/2019.1 && sudo mkdir /opt/Xilinx/Petalinux/2018.3 && sudo chown -R ckayhan:ckayhan /opt/Xilinx
##tar xfv /mnt/sda1/VIVADO/2019.1/Xilinx_Vivado_SDK_2019.1_0524_1430.tar.gz && cd Xilinx_Vivado_SDK_2019.1_0524_1430
##./xsetup -b ConfigGen
##./xsetup --agree XilinxEULA,3rdPartyEULA,WebTalkTerms --batch Install --config ~/.Xilinx/install_config.txt
##cd /opt/Xilinx/Vivado/2019.1/data/xicom/cable_drivers/lin64/install_script/install_drivers && sudo ./install_drivers
##cd /opt/Xilinx/Vivado/2019.1/patches && unzip -x /mnt/sda1/VIVADO/2019.1/AR73126_vivado_2019_1_preliminary_rev1.zip -d AR73126_vivado_2019_1_preliminary_rev1
##sudo mkdir -p /mnt/nfsroot
##echo '/mnt/nfsroot 10.233.1.0/24(rw,sync,no_root_squash,no_subtree_check)' | sudo tee -a /etc/exports && sudo exportfs -a && sudo exportfs -v
##sudo chown -R nobody:nogroup /mnt/nfsroot
##sudo chmod -R 777 /mnts/nfsroot
##sudo mkdir -p /opt/Petalinux && sudo chown -R ckayhan:ckayhan /opt/Petalinux
#
## how to check Ubuntu version on bash
#cat /etc/os-release
#
### install oracle virtualbox 6 on centos 7
##sudo yum update
##sudo yum install –y patch gcc kernel-headers kernel-devel make perl wget
##sudo reboot
##sudo wget http://download.virtualbox.org/virtualbox/rpm/el/virtualbox.repo -P /etc/yum.repos.d
##sudo yum install VirtualBox-6.0
##sudo systemctl status vboxdrv
##wget http://download.virtualbox.org/virtualbox/6.0.4/Oracle_VM_VirtualBox_Extension_Pack-6.0.4.vbox-extpack
##sudo VBoxManage extpack install Oracle_VM_VirtualBox_Extension_Pack-6.0.4.vbox-extpack
##
### install iwconfig on centos
##sudo yum install wireless-tools
##
### enable wifi on centos ardadurustpc
##sudo tee /etc/modprobe.d/ideapad.conf <<< "blacklist ideapad_laptop"
##
###
##sudo yum install network-manager-applet
##
### how to enable exfat filesystem on centos
##sudo rpm -v --import http://li.nux.ro/download/nux/RPM-GPG-KEY-nux.ro
##sudo rpm -Uvh http://li.nux.ro/download/nux/dextop/el7/x86_64/nux-dextop-release-0-5.el7.nux.noarch.rpm
##sudo yum install exfat-utils fuse-exfat
#
## how to learn open files of mounted drives
#sudo lsof +D /mnt/SanDisk_256GB
#
## install wireshark on centos
##sudo yum install wireshark wireshark-gnome -y
#
## add domain to firewall whitelist on centos
#sudo firewall-cmd --permanent --zone=trusted --add-source=10.230.1.0/24
#sudo firewall-cmd --reload
#
## how to compile opencv4 on centos
#mkdir opencv4_build && cd opencv4_build
#git clone https://github.com/opencv/opencv.git
#git clone https://github.com/opencv/opencv_contrib.git
#cd opencv && mkdir build && cd build
#git checkout 4.1.1
#cmake3 -DCMAKE_BUILD_TYPE=RELEASE -DCMAKE_INSTALL_PREFIX=/usr/local -DWITH_V4L=ON -DBUILD_TESTS=OFF -DBUILD_ZLIB=ON -DBUILD_JPEG=ON -DWITH_JPEG=ON -DWITH_PNG=ON -DBUILD_EXAMPLES=OFF -DINSTALL_C_EXAMPLES=OFF -DINSTALL_PYTHON_EXAMPLES=OFF -DWITH_OPENEXR=OFF -DBUILD_OPENEXR=OFF -DWITH_EIGEN=OFF -DCMAKE_CXX_COMPILER=/home/xilinx/Vivado/2018.3/tps/lnx64/gcc-6.2.0/bin/g++ ..
#make -j32
#sudo make install
#
## how to program myir7015 qspi flash
#program_flash -target_name jsn-JTAG-HS2-210249A73CF5-4ba00477-0 -f BOOT.bin -fsbl fsbl.elf -flash_type qspi-x4-single -blank_check -verify -url TCP:localhost:3121 -offset 0
#
## how to set hostname permanently
#sudo hostnamectl set-hostname ardadurustpc
#
## how to config git diff tool
#git config --global diff.tool gvimdiff
#git config --global --add difftool.prompt false
#
## how to change boot mode to JTAG from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x0100
#rst -system
#
## how to change boot mode to QUAD-SPI(24b) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x1100 
#rst -system
#
## how to change boot mode to QUAD-SPI(32b) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x2100 
#rst -system
#
## how to change boot mode to SD0(2.0) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x3100 
#rst -system
#
## how to change boot mode to NAND from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x4100 
#rst -system
#
## how to change boot mode to SD1(2.0) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x5100 
#rst -system
#
## how to change boot mode to eMMC(1.8V) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x6100 
#rst -system
#
## how to change boot mode to USB(2.0) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x7100 
#rst -system
#
## how to change boot mode to PJTAG(MIO#0) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x8100 
#rst -system
#
## how to change boot mode to PJTAG(MIO#1) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0x9100 
#rst -system
#
## how to change boot mode to SD1(3.0) from xsct console
#targets -set -nocase -filter {name =~ "*PSU*"}
#stop
#mwr  0xff5e0200 0xe100 
#rst -system
#
## how to change ARTF UART to PS UART1 on petalinux
#petalinux-config -> ARM Trusted Firmware Compilation Configuration -> extra ATF compilation settings -> ZYNQMP_CONSOLE=cadence1
#
## how to format sd card
#sudo mkfs.vfat -F 32 -n BOOT /dev/sdc1
#sudo mkfs.ext4 -L ROOT /dev/sdc2
#
## add static ip and route to dev
#sudo ip a add 10.233.1.62/24 dev enp7s0
#sudo ip r add 10.233.1.0/24 via 10.233.1.1 dev enp7s0
#
## how to add gstreamer avdec_h256 on centos 7
##sudo yum install https://download1.rpmfusion.org/free/el/rpmfusion-free-release-7.noarch.rpm -y
##sudo yum install http://download1.rpmfusion.org/free/el/updates/7/x86_64/g/gstreamer1-libav-1.10.4-2.el7.x86_64.rpm -y
#
## how to install tftp server on Ubuntu 16.04
#sudo apt install -y atftpd
#sudo sed -e 's/^USE_INETD=true/USE_INETD=false/g' -i /etc/default/atftpd
#sudo systemctl enable atftpd
#sudo systemctl restart atftpd
#
## Ubuntu/Debian xsct,Petalinux,tclsh rlwrap xterm problem
## rlwrap: warning: your $TERM is 'xterm' but rlwrap couldn't find it in the terminfo database. Expect some problems
#sudo cp -f /lib/terminfo/x/xterm /usr/share/terminfo/x
#
## How to install vim 8.2 on Ubuntu 16.04-4
#sudo add-apt-repository ppa:jonathonf/vim
#sudo apt update
#sudo apt upgrade
#
#mount vfat format usb to prevent permission denied
#sudo mount -o uid=1000,gid=1000 /dev/sdb1 /mnt/ASISGUARD2
